import React, { Fragment } from 'react';

const Spinner = () => {
    return (
        <Fragment>
            <h4 style={{ textAlign: 'center' }}>...Loading</h4>
        </Fragment>
    );
};

export default Spinner;
